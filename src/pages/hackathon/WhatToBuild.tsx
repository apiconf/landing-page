import { Section } from './Section';

export function WhatToBuild() {
  return (
    <Section
      id="what-to-build"
      title="What to build?"
      lead="Your project could improve commerce, healthcare, education, logistics, productivity, entertainment, developer experience, internal operations, or something completely unexpected. If Monnify APIs help power your solution, we want to see it."
    />
  );
}
